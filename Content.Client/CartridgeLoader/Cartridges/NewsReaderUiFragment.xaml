<cartridges:NewsReaderUiFragment xmlns:cartridges="clr-namespace:Content.Client.CartridgeLoader.Cartridges"
                               xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                               xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                               xmlns:customControls="clr-namespace:Content.Client.Administration.UI.CustomControls"
                               xmlns="https://spacestation14.io"
                               Margin="1 0 2 0"
                               Orientation="Vertical"
                               HorizontalExpand="True"
                               VerticalExpand="True">
    <PanelContainer StyleClasses="BackgroundDark"></PanelContainer>
    <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5,5,5,5">
        <Button
            Name="Prev"
            MinWidth="64"
            Disabled="True"
            HorizontalAlignment="Left"
            Text="{Loc 'news-read-ui-prev-text'}"
            ToolTip="{Loc 'news-read-ui-prev-tooltip'}"
            Access="Public"
            HorizontalExpand="True"/>
        <Button
            Name="Next"
            MinWidth="64"
            Disabled="True"
            HorizontalAlignment="Right"
            Text="{Loc 'news-read-ui-next-text'}"
            ToolTip="{Loc 'news-read-ui-next-tooltip'}"/>
    </BoxContainer>
    <controls:StripeBack Name="ArticleNameContainer">
        <PanelContainer>
            <Label Name="PageNum" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4,0,0,0"/>
            <Label Name="PageName" Align="Center"/>
        </PanelContainer>
    </controls:StripeBack>
    <PanelContainer VerticalExpand="True">
        <PanelContainer.PanelOverride>
            <gfx:StyleBoxFlat BackgroundColor="#80808005" />
        </PanelContainer.PanelOverride>
        <ScrollContainer
                Name="PageTextScroll"
                HScrollEnabled="False"
                HorizontalExpand="True"
                MinSize="0 0"
                VerticalExpand="True">
            <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                <RichTextLabel Margin="8,8,8,8" Name="PageText" VerticalAlignment="Top"/>
                <BoxContainer Name="CommentsSection" Orientation="Vertical" HorizontalExpand="True">
                    <PanelContainer Margin="4 4 4 0">
                        <PanelContainer.PanelOverride>
                            <gfx:StyleBoxFlat BorderColor="#555555" BorderThickness="0 1 0 0"/>
                        </PanelContainer.PanelOverride>
                    </PanelContainer>
                    <Label Name="CommentsHeader" Margin="8 4 8 2"
                           FontColorOverride="#aaaaaa" StyleClasses="LabelSmall"/>
                    <BoxContainer Name="CommentsList" Orientation="Vertical" HorizontalExpand="True"/>
                    <Label Name="NoCommentsLabel" Margin="8 2 8 6"
                           FontColorOverride="#777777" StyleClasses="LabelSmall"/>
                </BoxContainer>
            </BoxContainer>
        </ScrollContainer>
    </PanelContainer>
    <BoxContainer Name="CommentInputBox" Orientation="Horizontal" HorizontalExpand="True" Margin="4 2 4 2">
        <LineEdit Name="CommentInput" HorizontalExpand="True"
                  PlaceHolder="{Loc news-comment-placeholder}"/>
        <Button Name="PostCommentButton" Text="{Loc news-comment-post-button}"
                MinWidth="46" Margin="2 0 0 0"/>
    </BoxContainer>
    <BoxContainer Orientation="Horizontal" HorizontalExpand="True" Margin="5,5,5,5">
        <Button
            Name="NotificationSwitch"
            ToolTip="{Loc news-reader-ui-mute-tooltip}"
            MinWidth="20"/>
        <RichTextLabel Margin="5,2,2,2" Name="ShareTime" VerticalAlignment="Top"/>
        <customControls:VSeparator Margin="2 0"/>
        <RichTextLabel Margin="5,2,2,2" Name="Author" VerticalAlignment="Top" HorizontalAlignment="Right"/>
    </BoxContainer>
</cartridges:NewsReaderUiFragment>
