# DEPRECATED (upstream PR#42476): BaseMobVox / MobVoxDummy were orphaned dead code.
# Their parents (BaseMobSpeciesOrganic, BaseSpeciesDummy) were removed by the upstream refactor,
# and nothing referenced these entities — the official Vox (MobVox / AppearanceVox in
# Resources/Prototypes/Body/Species/vox.yml) supersedes them. Removed to avoid load errors.
# Old content recoverable from git history.
